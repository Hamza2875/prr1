extends layout

block content
  h1 My shopping Cart
  - var total=0;
    table.table
      thead
        th Name
        th Actions
      tbody
        each item in cart
          - total += Number(item.price)
          tr
            td=item.FName
            td
              a(href="/novels/cart/remove/"+item._id, class="btn btn-danger") Remove Item
